apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: video-platform
  labels:
    app: dashboard
spec:
  selector:
    app: dashboard
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
